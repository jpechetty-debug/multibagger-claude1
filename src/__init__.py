"""
Deprecated compatibility package.

The canonical application entry point is the repo-root `main.py`.
Only `src.main` remains as a temporary shim for legacy imports.
New code should not add dependencies on the `src.*` tree.
"""
