# ── Python ───────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
*.egg
.pytest_cache/
.mypy_cache/
.ruff_cache/
htmlcov/
.coverage
.coverage.*
coverage.xml
*.cover

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Environment & secrets — NEVER commit these ────────────────────────────────
.env
.env.local
.env.production
*.pem
*.key
secrets/

# ── Node / web-ui ─────────────────────────────────────────────────────────────
web-ui/node_modules/
web-ui/.next/
web-ui/dist/
web-ui/.cache/
node_modules/

# ── Runtime databases & model artifacts ───────────────────────────────────────
*.db
*.db-shm
*.db-wal
*.pkl
*.joblib
market_regime_hmm.pkl
celerybeat-schedule
celerybeat.pid

# ── Docker ────────────────────────────────────────────────────────────────────
# Don't ignore docker-compose.yml — it's part of the repo.
# But do ignore generated volumes and overrides.
docker-compose.override.yml

# ── IDE ───────────────────────────────────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# ── Logs & reports ────────────────────────────────────────────────────────────
logs/
*.log
reports/*.html
reports/*.json
ops/daily_reports/
ops/institutional_daily_scorecard.json

# ── Debug / scratch outputs (never commit these) ─────────────────────────────
scratch/
backup.bat
health_check*.txt
checklist_output.txt
test_output.txt
test_results.txt
check_fail*.txt
disq_fail*.txt
final_fail*.txt
liquidity.json
paper_trade_signals.json
modules/analyst_consensus.json
delisted_candidates.txt
tests/*_log.txt
reports_cache/
ops/ticker_list_*.txt
ops/audit_result.txt
_fns.txt

# ── Data directory (populated at runtime) ────────────────────────────────────
data/
runtime/

# Agent State
.gemini/

# ── Artifacts that must never be committed ────────────────────────────────────
*.zip
*.bak
*.bak_fix*
*.bak_*
extracted_*/

# ── Output / result files (populated at runtime) ──────────────────────────────
*.csv
master_picks_report.*
screener_results.*

# ── Wrong-repo intraday bot files (moved to sovereign-engine repo) ─────────────
sovereign-cli.py
sovereign_analyst_reports.py
sovereign_mcp.py
 
test_results.txt
schema_output.txt
alpha_report.md
backtest_report.md
liquidity_report.md
 
git_files.txt
tests_out.txt
refactor_script.py

# AI Scaffolding & Bloat
.agent/
.pytest_tmp/
*.bak
extracted_*/
